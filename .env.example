# Copy this file to `.env` and set the real values locally (do NOT commit your real .env)
# Gemini API (provide the HTTP endpoint and API key). Do NOT commit your real key.
GEMINI_API_ENDPOINT=
GEMINI_API_KEY=

# Database URL example (optional override)
#DATABASE_URL=postgresql://postgres:password@localhost:5432/caretaker
